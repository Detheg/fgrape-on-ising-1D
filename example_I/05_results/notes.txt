Simulation details:

N_chains = 2, n = 2
jump_ops = [gamma * embed(sigmam(), idx, N_chains*n) for idx in range(N_chains*n)] with gamma=0.25
initial and target states were (alpha*|01> + beta*|10>) with random, complex alpha, beta.
